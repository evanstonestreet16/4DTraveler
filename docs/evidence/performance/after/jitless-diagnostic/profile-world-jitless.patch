--- a/profile-world.mjs
+++ b/profile-world.mjs
@@ -90,6 +90,8 @@
   quality,
   warmupCycles: warmupCount,
   deviceScaleFactor: dpr,
+  diagnosticOnly: true,
+  additionalBrowserFlags: ['--js-flags=--jitless'],
 };
 
 /** All handles are weakly held: measurement must not keep discarded worlds alive. */
@@ -613,8 +615,8 @@
 const browser = await chromium.launch({
   headless: !environment.headed,
   args: software
-    ? ['--use-angle=swiftshader', '--enable-unsafe-swiftshader']
-    : [],
+    ? ['--use-angle=swiftshader', '--enable-unsafe-swiftshader', '--js-flags=--jitless']
+    : ['--js-flags=--jitless'],
 });
 try {
   const browserCdp = await browser.newBrowserCDPSession();
