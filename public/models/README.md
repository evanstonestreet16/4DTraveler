# Future model assets

Dummy v0 uses primitives from world data and requires no models. Future `.glb` assets should preserve the world contract's `sceneObjectId` mappings for object selection. Blender is an optional authoring tool, never a runtime dependency.
